-- A Room (test)

-- called on init
function init()
	mgSetSpeed(1.0, 10.0)
	
	buildWallsAndFloor(6.0, 5.0, 150.0)
	
	mgLength(150.0)
end

-- called on every tick
function tick()
end

-- called on free
function free()
end
